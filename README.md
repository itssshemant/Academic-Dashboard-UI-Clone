
  # Academic Dashboard UI Design

  This is a code bundle for Academic Dashboard UI Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
